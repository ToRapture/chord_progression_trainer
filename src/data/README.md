# Data Directory

This directory is reserved for future data files, such as:

- User progress history (localStorage, IndexedDB)
- Custom progression templates
- Export/report data
- Sample audio metadata

Currently, all progression data is embedded in the progression library source files.
